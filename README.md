Start using 
```
node index.js
```
env variables:
```
key : insert access key id for configuring aws
secret: insert secret access key for configuring aws
region: insert aws region
bucket_name: insert bucket_name for s3

image url can be a public image url or an image pre uploaded in your bucket


